apiVersion: v1
kind: Service
metadata:
  name: enterprise-app
  labels:
    app: enterprise-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: enterprise-app 