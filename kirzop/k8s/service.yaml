apiVersion: v1
kind: Service
metadata:
  name: kirzop-service
  labels:
    app: kirzop
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: kirzop 
