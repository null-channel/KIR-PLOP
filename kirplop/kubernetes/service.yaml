apiVersion: v1
kind: Service
metadata:
  name: tree-controller
  labels:
    app: tree-controller
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: tree-controller 